"""
yield #生成器函数发送协议
lambda args: expression # 生成匿名函数
x if y else z  #三元表达式
x or y  #逻辑或
x and y #逻辑与
not x #逻辑非
x in y, x not in y # 成员关系(可迭代对象，集合)
x is y, x is not y # 对象实体测试
x < y, x <=y, x> y, x >= y  #大小比较
x == y, x != y
x | y
x ^ y
x & y
x << y, x >> y
x + y, x - y
x * y, x% y, x / y, x // y   #最后是floor除法
-x, +x
~x # 按位求补
x ** y  # 幂运算
x[i]
x[i:j:k]
x(...)
x.attr
(...) # 元祖，表达式，生成器表达式
[...] # 列表，列表解析
{...} # 字典，集合
"""